export enum Roles {
  USER,
  ADMIN,
}

export enum TransactionStatus {
  PENDING,
  COMPLETED,
}

export enum AppontmentStatus {
  PENDING,
  COMPLETED,
}

export enum ListingStatus {
  PENDING,
  ACTIVE,
  SOLD,
}
